anthropic>=0.40.0
python-dotenv>=1.0.0
scrapegraphai>=1.76.0
# Pinned: scrapegraphai 1.76.0 imports ChatOllama from langchain_community,
# which was removed in langchain-community>=0.4.1. 0.4.0 still has it and
# satisfies scrapegraphai's own ">=0.4.0" constraint.
langchain-community==0.4.0
